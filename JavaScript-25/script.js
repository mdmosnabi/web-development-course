// Exesice JavaScript Promise

//1. Create a function checkNumber(num) that returns a Promise.

//2. The Promise should resolve with the message "Even Number" if num is even, and reject with "Odd Number" if num is odd.

//3. Test this function with then() and catch() methods to handle both resolve and reject cases.